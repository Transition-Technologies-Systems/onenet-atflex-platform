package pl.com.tt.flex.server.domain.user.registration.enumeration;

public enum FspUserRegGeneratedCommentText {
    READ_BY_ADMIN,
    ACCEPTED_BY_ADMIN,
    REJECTED_BY_ADMIN,
    WITHDRAWN_BY_USER
}
